import XCTest
@testable import hypercut_native_networking

final class hypercut_native_networkingTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(hypercut_native_networking().text, "Hello, World!")
    }
}
